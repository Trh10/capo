{
  "name": "capo-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Coque Android CAPO Studio — charge le site en production",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "prepare:android": "node scripts/prepare.mjs",
    "sync": "node scripts/copy-splash.mjs && node scripts/inject-config.mjs && npx cap sync android && npm run icons && npm run prepare:android",
    "open:android": "npx cap open android",
    "build:apk": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.4.2",
    "@capacitor/splash-screen": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "sharp": "^0.34.3",
    "typescript": "^5.8.2"
  }
}
